name: 🏷️ Release

on:
  release:
    types:
      - created
      - edited
      - published

permissions: {}

jobs:
  build:
    name: 🏗️ Build
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      # Fail the release before anything deploys: a malformed targets var
      # would otherwise only surface when a color job's matrix expands,
      # after alpha and beta already shipped the new image. length > 0 also
      # rejects "[]", which is truthy so the matrix defaults below would not
      # catch it and the color would silently get zero deploy legs.
      - name: 🔍 Validate deploy targets
        env:
          DEPLOY_TARGETS_BETA: ${{ vars.DEPLOY_TARGETS_BETA }}
          DEPLOY_TARGETS_BLUE: ${{ vars.DEPLOY_TARGETS_BLUE }}
          DEPLOY_TARGETS_GREEN: ${{ vars.DEPLOY_TARGETS_GREEN }}
        run: |
          set -euo pipefail
          for name in DEPLOY_TARGETS_BETA DEPLOY_TARGETS_BLUE DEPLOY_TARGETS_GREEN; do
            value="${!name}"
            [ -z "$value" ] && continue
            if ! printf '%s' "$value" | jq -e 'type == "array" and length > 0 and all(.[]; (.host | type == "string") and (.subdomain | type == "string") and (.letter | type == "string") and (.numWorkers | type == "number"))' > /dev/null; then
              echo "Error: repository var ${name} must be a non-empty JSON array of {host, subdomain, letter, numWorkers[, gameHost]} objects, got: ${value}"
              exit 1
            fi
          done
          # Blue and green are one site, and a letter names one host there for
          # good (the registry binds it), so two targets sharing a letter would
          # leave one of them refused at check-in and never flagged latest.
          # Catch it here, before alpha ships. Unset vars fall back to the
          # single-box defaults below, which are known to be distinct.
          dupes="$(printf '%s\n%s' "${DEPLOY_TARGETS_BLUE:-[]}" "${DEPLOY_TARGETS_GREEN:-[]}" \
            | jq -rs 'add | [.[].letter] | group_by(.) | map(select(length > 1) | .[0]) | .[]')"
          if [ -n "$dupes" ]; then
            echo "Error: instance letter(s) used by more than one blue/green target: ${dupes}"
            exit 1
          fi
      - name: 🔗 Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ vars.GHCR_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}
      - id: build
        env:
          GHCR_REPO: openfront-prod
          GHCR_USERNAME: ${{ vars.GHCR_USERNAME }}
          RELEASE_BODY: ${{ github.event.release.body }}
          RELEASE_NAME: ${{ github.event.release.name }}
          RELEASE_TAG_NAME: ${{ github.event.release.tag_name }}
          ADDITIONAL_VERSION_TAG: ${{ github.event.action == 'published' && 'latest' || '' }}
        run: |
          set -euxo pipefail
          cat <<EOF >> $GITHUB_STEP_SUMMARY
          Name: ${RELEASE_NAME}
          Tag: ${RELEASE_TAG_NAME}
          Changelog:
          ${RELEASE_BODY}
          EOF
          # Third argument is VERSION_TXT, which build.sh writes to
          # resources/version.txt and the client bakes into the bundle as the
          # version the footer and nav bar show. It used to be RELEASE_NAME --
          # free text typed into the GitHub Release UI, so the shipped version
          # label was whatever someone happened to title the release. The TAG
          # is the build's actual identity and is already validated as the
          # image tag on the line's second argument, so use it. RELEASE_NAME
          # stays in the job summary above, where free text belongs.
          ./build.sh prod "${RELEASE_TAG_NAME}" "${RELEASE_TAG_NAME}" "${RELEASE_BODY}" /tmp/build-metadata.json
          IMAGE_ID=$(jq -r '."containerimage.digest"' /tmp/build-metadata.json)
          echo "IMAGE_ID=${IMAGE_ID}" >> $GITHUB_OUTPUT
          echo "Image ID: \`${IMAGE_ID}\`" >> $GITHUB_STEP_SUMMARY
    outputs:
      IMAGE_ID: ${{ steps.build.outputs.IMAGE_ID }}

  deploy-alpha:
    name: 🧪 Deploy to alpha
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [build]
    steps:
      - uses: actions/checkout@v7
      # deploy.sh keyscans the target it resolves, so this step only
      # installs the key.
      - name: 🔑 Create SSH private key
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          set -euxo pipefail
          mkdir -p ~/.ssh
          echo "${SSH_PRIVATE_KEY}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
      - name: 🚀 Deploy image
        env:
          GHCR_REPO: openfront-prod
          GHCR_USERNAME: ${{ vars.GHCR_USERNAME }}
          DOMAIN: ${{ vars.DOMAIN }}
          CDN_BASE: ${{ vars.CDN_BASE }}
          IMAGE_ID: ${{ needs.build.outputs.IMAGE_ID }}
          OTEL_EXPORTER_OTLP_ENDPOINT: ${{ secrets.OTEL_EXPORTER_OTLP_ENDPOINT }}
          OTEL_AUTH_HEADER: ${{ secrets.OTEL_AUTH_HEADER }}
          API_KEY: ${{ secrets.API_KEY }}
          ADMIN_BOT_API_KEY: ${{ secrets.ADMIN_BOT_API_KEY }}
          LOBBY_COORDINATOR: ${{ vars.LOBBY_COORDINATOR }}
          TURNSTILE_SITE_KEY: ${{ vars.TURNSTILE_SITE_KEY }}
          # Runtime env → the page's BOOTSTRAP_CONFIG (see deploy.yml).
          # Environment-scoped: the prod-* environments carry the live-mode
          # key; alpha has no environment and inherits the repo-level
          # test-mode key, matching its test-mode API.
          STRIPE_PUBLISHABLE_KEY: ${{ vars.STRIPE_PUBLISHABLE_KEY }}
          SERVER_HOST_STAGING: ${{ secrets.SERVER_HOST_STAGING }}
          SERVER_HOSTS_JSON: ${{ secrets.SERVER_HOSTS_JSON }}
          SSH_KEY: ~/.ssh/id_rsa
        run: |
          set -euxo pipefail
          bash -x ./deploy.sh staging staging "${IMAGE_ID}" alpha
      - name: ⏳ Wait for deployment to start
        env:
          FQDN: alpha.${{ vars.DOMAIN }}
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          echo "::group::Wait for deployment to start"
          set -euxo pipefail
          while [ "$(curl -s -H "X-API-Key: ${API_KEY}" https://${FQDN}/commit.txt)" != "${GITHUB_SHA}" ]; do
            if [ "$SECONDS" -ge 300 ]; then
              echo "Timeout: deployment did not start within 5 minutes"
              exit 1
            fi
            sleep 10
          done
          echo "Deployment started in ${SECONDS} seconds" >> $GITHUB_STEP_SUMMARY
          echo "::endgroup::"

  deploy-beta:
    name: 🐞 Deploy to beta (${{ matrix.target.host }})
    runs-on: ubuntu-latest
    needs: [build, deploy-alpha]
    timeout-minutes: 30
    environment: prod-beta
    # One leg per machine, from the DEPLOY_TARGETS_BETA REPOSITORY var (see
    # deploy-blue for the format and why it must be repository-level).
    # Defaults to today's single box.
    strategy:
      max-parallel: 1
      matrix:
        target: ${{ fromJSON(vars.DEPLOY_TARGETS_BETA || '[{"host":"falk2","subdomain":"beta","letter":"e","numWorkers":4}]') }}
    steps:
      - uses: actions/checkout@v7
      # deploy.sh keyscans the target it resolves, so this step only
      # installs the key.
      - name: 🔑 Create SSH private key
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          set -euxo pipefail
          mkdir -p ~/.ssh
          echo "${SSH_PRIVATE_KEY}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
      - name: 🚀 Deploy image
        env:
          GHCR_REPO: ${{ vars.GHCR_REPO }}
          GHCR_USERNAME: ${{ vars.GHCR_USERNAME }}
          DOMAIN: ${{ vars.DOMAIN }}
          CDN_BASE: ${{ vars.CDN_BASE }}
          IMAGE_ID: ${{ needs.build.outputs.IMAGE_ID }}
          OTEL_EXPORTER_OTLP_ENDPOINT: ${{ secrets.OTEL_EXPORTER_OTLP_ENDPOINT }}
          OTEL_AUTH_HEADER: ${{ secrets.OTEL_AUTH_HEADER }}
          API_KEY: ${{ secrets.API_KEY }}
          ADMIN_BOT_API_KEY: ${{ secrets.ADMIN_BOT_API_KEY }}
          # This container's identity, from its deploy target entry (see the
          # matrix comment on deploy-blue and deploy.sh, "identity").
          INSTANCE_LETTER: ${{ matrix.target.letter }}
          NUM_WORKERS: ${{ matrix.target.numWorkers }}
          GAME_HOST: ${{ matrix.target.gameHost || '' }}
          LOBBY_COORDINATOR: ${{ vars.LOBBY_COORDINATOR }}
          TURNSTILE_SITE_KEY: ${{ vars.TURNSTILE_SITE_KEY }}
          # Runtime env → the page's BOOTSTRAP_CONFIG (see deploy.yml).
          # Environment-scoped: the prod-* environments carry the live-mode
          # key; alpha has no environment and inherits the repo-level
          # test-mode key, matching its test-mode API.
          STRIPE_PUBLISHABLE_KEY: ${{ vars.STRIPE_PUBLISHABLE_KEY }}
          SERVER_HOST_FALK2: ${{ secrets.SERVER_HOST_FALK2 }}
          SERVER_HOSTS_JSON: ${{ secrets.SERVER_HOSTS_JSON }}
          TARGET_HOST: ${{ matrix.target.host }}
          TARGET_SUBDOMAIN: ${{ matrix.target.subdomain }}
          SSH_KEY: ~/.ssh/id_rsa
        run: |
          set -euxo pipefail
          ./deploy.sh prod "${TARGET_HOST}" "${IMAGE_ID}" "${TARGET_SUBDOMAIN}"
      - name: ⏳ Wait for deployment to start
        env:
          FQDN: ${{ matrix.target.subdomain }}.${{ vars.DOMAIN }}
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          echo "::group::Wait for deployment to start"
          set -euxo pipefail
          while [ "$(curl -s -H "X-API-Key: ${API_KEY}" https://${FQDN}/commit.txt)" != "${GITHUB_SHA}" ]; do
            if [ "$SECONDS" -ge 300 ]; then
              echo "Timeout: deployment did not start within 5 minutes"
              exit 1
            fi
            sleep 10
          done
          echo "Deployment started in ${SECONDS} seconds" >> $GITHUB_STEP_SUMMARY
          echo "::endgroup::"

  deploy-blue:
    name: 🔵 Deploy to blue (${{ matrix.target.host }})
    runs-on: ubuntu-latest
    needs: [build, deploy-alpha]
    timeout-minutes: 30
    environment: prod-blue
    # One leg per machine in the blue fleet, from the DEPLOY_TARGETS_BLUE
    # REPOSITORY var. Each entry is one container's whole identity:
    #   [{"host":"falk2","subdomain":"blue","letter":"c","numWorkers":20},
    #    {"host":"nbg2","subdomain":"blue","letter":"f","numWorkers":20,
    #     "gameHost":"blue.nbg2.openfront.io"}]
    # letter leads every game id the container mints (append-only, never
    # reused); numWorkers is frozen while the letter has live games; gameHost
    # is the name clients open sockets to, defaulting to
    # <subdomain>.<domain> — pass it to put the machine in the name so every
    # box carries a plain blue and green (deploy.sh, "identity").
    # Repository-level and per-color on purpose: GitHub expands
    # strategy.matrix before the job's environment exists, so an
    # environment-scoped var is invisible here and would silently fall back
    # to the single-box default — and one shared name would feed the same
    # list to both colors. host is a machine name deploy.sh resolves via the
    # SERVER_HOSTS_JSON secret (lowercase keys; SERVER_HOST_FALK2 is the
    # only legacy secret wired through in CI); subdomain is that machine's
    # slot. Defaults to today's single box. Sequential (and
    # stopping on the first failure) so a bad image or a dead machine halts
    # the rollout after one leg instead of taking the whole color down.
    strategy:
      max-parallel: 1
      matrix:
        target: ${{ fromJSON(vars.DEPLOY_TARGETS_BLUE || '[{"host":"falk2","subdomain":"blue","letter":"c","numWorkers":20}]') }}
    steps:
      - uses: actions/checkout@v7
      # deploy.sh keyscans the target it resolves, so this step only
      # installs the key.
      - name: 🔑 Create SSH private key
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          set -euxo pipefail
          mkdir -p ~/.ssh
          echo "${SSH_PRIVATE_KEY}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
      - name: 🚀 Deploy image
        env:
          GHCR_REPO: ${{ vars.GHCR_REPO }}
          GHCR_USERNAME: ${{ vars.GHCR_USERNAME }}
          DOMAIN: ${{ vars.DOMAIN }}
          # Blue/green sit behind the openfront.io load balancer: see ServerEnv.siteHost.
          SITE_HOST: ${{ vars.DOMAIN }}
          CDN_BASE: ${{ vars.CDN_BASE }}
          IMAGE_ID: ${{ needs.build.outputs.IMAGE_ID }}
          OTEL_EXPORTER_OTLP_ENDPOINT: ${{ secrets.OTEL_EXPORTER_OTLP_ENDPOINT }}
          OTEL_AUTH_HEADER: ${{ secrets.OTEL_AUTH_HEADER }}
          API_KEY: ${{ secrets.API_KEY }}
          ADMIN_BOT_API_KEY: ${{ secrets.ADMIN_BOT_API_KEY }}
          # This container's identity, from its deploy target entry (see the
          # matrix comment on deploy-blue and deploy.sh, "identity").
          INSTANCE_LETTER: ${{ matrix.target.letter }}
          NUM_WORKERS: ${{ matrix.target.numWorkers }}
          GAME_HOST: ${{ matrix.target.gameHost || '' }}
          LOBBY_COORDINATOR: ${{ vars.LOBBY_COORDINATOR }}
          TURNSTILE_SITE_KEY: ${{ vars.TURNSTILE_SITE_KEY }}
          # Runtime env → the page's BOOTSTRAP_CONFIG (see deploy.yml).
          # Environment-scoped: the prod-* environments carry the live-mode
          # key; alpha has no environment and inherits the repo-level
          # test-mode key, matching its test-mode API.
          STRIPE_PUBLISHABLE_KEY: ${{ vars.STRIPE_PUBLISHABLE_KEY }}
          SERVER_HOST_FALK2: ${{ secrets.SERVER_HOST_FALK2 }}
          SERVER_HOSTS_JSON: ${{ secrets.SERVER_HOSTS_JSON }}
          TARGET_HOST: ${{ matrix.target.host }}
          TARGET_SUBDOMAIN: ${{ matrix.target.subdomain }}
          SSH_KEY: ~/.ssh/id_rsa
        run: |
          set -euxo pipefail
          ./deploy.sh prod "${TARGET_HOST}" "${IMAGE_ID}" "${TARGET_SUBDOMAIN}"
      - name: ⏳ Wait for deployment to start
        env:
          FQDN: ${{ matrix.target.subdomain }}.${{ vars.DOMAIN }}
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          echo "::group::Wait for deployment to start"
          set -euxo pipefail
          while [ "$(curl -s -H "X-API-Key: ${API_KEY}" https://${FQDN}/commit.txt)" != "${GITHUB_SHA}" ]; do
            if [ "$SECONDS" -ge 300 ]; then
              echo "Timeout: deployment did not start within 5 minutes"
              exit 1
            fi
            sleep 10
          done
          echo "Deployment started in ${SECONDS} seconds" >> $GITHUB_STEP_SUMMARY
          echo "::endgroup::"

  deploy-green:
    name: 🟢 Deploy to green (${{ matrix.target.host }})
    runs-on: ubuntu-latest
    needs: [build, deploy-alpha]
    timeout-minutes: 30
    environment: prod-green
    # One leg per machine in the green fleet, from the DEPLOY_TARGETS_GREEN
    # REPOSITORY var (see deploy-blue for the format and why it must be
    # repository-level and per-color):
    #   [{"host":"falk2","subdomain":"green","letter":"d","numWorkers":20},
    #    {"host":"nbg2","subdomain":"green","letter":"g","numWorkers":20,
    #     "gameHost":"green.nbg2.openfront.io"}]
    # Defaults to today's single box. Sequential (and stopping on the first
    # failure) so a bad image or a dead machine halts the rollout after one
    # leg instead of taking the whole color down.
    strategy:
      max-parallel: 1
      matrix:
        target: ${{ fromJSON(vars.DEPLOY_TARGETS_GREEN || '[{"host":"falk2","subdomain":"green","letter":"d","numWorkers":20}]') }}
    steps:
      - uses: actions/checkout@v7
      # deploy.sh keyscans the target it resolves, so this step only
      # installs the key.
      - name: 🔑 Create SSH private key
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          set -euxo pipefail
          mkdir -p ~/.ssh
          echo "${SSH_PRIVATE_KEY}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
      - name: 🚀 Deploy image
        env:
          GHCR_REPO: ${{ vars.GHCR_REPO }}
          GHCR_USERNAME: ${{ vars.GHCR_USERNAME }}
          DOMAIN: ${{ vars.DOMAIN }}
          # Blue/green sit behind the openfront.io load balancer: see ServerEnv.siteHost.
          SITE_HOST: ${{ vars.DOMAIN }}
          CDN_BASE: ${{ vars.CDN_BASE }}
          IMAGE_ID: ${{ needs.build.outputs.IMAGE_ID }}
          OTEL_EXPORTER_OTLP_ENDPOINT: ${{ secrets.OTEL_EXPORTER_OTLP_ENDPOINT }}
          OTEL_AUTH_HEADER: ${{ secrets.OTEL_AUTH_HEADER }}
          API_KEY: ${{ secrets.API_KEY }}
          ADMIN_BOT_API_KEY: ${{ secrets.ADMIN_BOT_API_KEY }}
          # This container's identity, from its deploy target entry (see the
          # matrix comment on deploy-blue and deploy.sh, "identity").
          INSTANCE_LETTER: ${{ matrix.target.letter }}
          NUM_WORKERS: ${{ matrix.target.numWorkers }}
          GAME_HOST: ${{ matrix.target.gameHost || '' }}
          LOBBY_COORDINATOR: ${{ vars.LOBBY_COORDINATOR }}
          TURNSTILE_SITE_KEY: ${{ vars.TURNSTILE_SITE_KEY }}
          # Runtime env → the page's BOOTSTRAP_CONFIG (see deploy.yml).
          # Environment-scoped: the prod-* environments carry the live-mode
          # key; alpha has no environment and inherits the repo-level
          # test-mode key, matching its test-mode API.
          STRIPE_PUBLISHABLE_KEY: ${{ vars.STRIPE_PUBLISHABLE_KEY }}
          SERVER_HOST_FALK2: ${{ secrets.SERVER_HOST_FALK2 }}
          SERVER_HOSTS_JSON: ${{ secrets.SERVER_HOSTS_JSON }}
          TARGET_HOST: ${{ matrix.target.host }}
          TARGET_SUBDOMAIN: ${{ matrix.target.subdomain }}
          SSH_KEY: ~/.ssh/id_rsa
        run: |
          set -euxo pipefail
          ./deploy.sh prod "${TARGET_HOST}" "${IMAGE_ID}" "${TARGET_SUBDOMAIN}"
      - name: ⏳ Wait for deployment to start
        env:
          FQDN: ${{ matrix.target.subdomain }}.${{ vars.DOMAIN }}
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          echo "::group::Wait for deployment to start"
          set -euxo pipefail
          while [ "$(curl -s -H "X-API-Key: ${API_KEY}" https://${FQDN}/commit.txt)" != "${GITHUB_SHA}" ]; do
            if [ "$SECONDS" -ge 300 ]; then
              echo "Timeout: deployment did not start within 5 minutes"
              exit 1
            fi
            sleep 10
          done
          echo "Deployment started in ${SECONDS} seconds" >> $GITHUB_STEP_SUMMARY
          echo "::endgroup::"
